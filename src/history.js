import {open} from 'node:fs/promises';
export async function readHistory(path,agent){let f;try{f=await open(path,'r');const stat=await f.stat(),size=Math.min(stat.size,1024*1024),buffer=Buffer.alloc(size);await f.read(buffer,0,size,stat.size-size);const lines=buffer.toString('utf8').split('\n');if(stat.size>size)lines.shift();const messages=[];for(const line of lines){let m;try{m=JSON.parse(line)}catch{continue}if(agent==='codex'&&m.type==='response_item'&&m.payload?.type==='message'&&['user','assistant'].includes(m.payload.role)){const text=(m.payload.content||[]).filter(x=>['input_text','output_text','text'].includes(x.type)).map(x=>x.text).join('\n');if(text)messages.push({role:m.payload.role,text,timestamp:m.timestamp,id:String(m.ordinal??m.timestamp)});}else if(agent==='claude'&&['user','assistant'].includes(m.type)){const c=m.message?.content;const text=typeof c==='string'?c:(c||[]).filter(x=>x.type==='text').map(x=>x.text).join('\n');if(text)messages.push({role:m.type,text,timestamp:m.timestamp,id:m.uuid});}}return messages.slice(-40);}catch(e){if(e.code==='ENOENT')return [];throw e}finally{await f?.close()}}
